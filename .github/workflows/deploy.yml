name: CI/CD
on:
  push:
    branches: [ "feature/homework" ]
  pull_request:
    branches: [ "feature/homework" ]

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: drf
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: Akoros781
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.13

      - name: Install Poetry
        run: |
          curl -sSL https://install.python-poetry.org | python3 -
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Install dependencies with Poetry
        run: |
          poetry install --no-root

      - name: Install redis-tools for redis-cli
        run: |
          sudo apt-get update
          sudo apt-get install -y redis-tools

      - name: Run migrations
        env:
          DB_NAME: drf
          DB_USER: postgres
          DB_PASSWORD: Akoros781
          DB_HOST: localhost
          DB_PORT: 5432
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
          STRIPE_PUBLISHABLE_KEY: ${{ secrets.STRIPE_PUBLISHABLE_KEY }}
          CELERY_BROKER_URL: redis://localhost:6379/0
          CELERY_RESULT_BACKEND: redis://localhost:6379/0
          REDIS_HOST: localhost
          REDIS_PORT: 6379
          REDIS_DB: 0
          DEBUG: True
        run: |
          poetry run python manage.py migrate

      - name: Run tests
        env:
          DB_NAME: drf
          DB_USER: postgres
          DB_PASSWORD: Akoros781
          DB_HOST: localhost
          DB_PORT: 5432
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
          STRIPE_PUBLISHABLE_KEY: ${{ secrets.STRIPE_PUBLISHABLE_KEY }}
          CELERY_BROKER_URL: redis://localhost:6379/0
          CELERY_RESULT_BACKEND: redis://localhost:6379/0
          REDIS_HOST: localhost
          REDIS_PORT: 6379
          REDIS_DB: 0
          DEBUG: True
        run: |
          poetry run python manage.py test

  build-and-push:
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/feature/homework'

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_HUB_USERNAME }}/drf-project:latest
            ${{ secrets.DOCKER_HUB_USERNAME }}/drf-project:${{ github.sha }}

  deploy:
    runs-on: ubuntu-latest
    needs: [ test, build-and-push ] # Зависит от тестов и сборки/пуша образа
    if: github.ref == 'refs/heads/feature/homework'

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # --- ШАГ 1: Копирование docker-compose.yaml на сервер ---
      - name: Copy docker-compose.yaml to server
        # Укажите конкретную стабильную версию, например, v1.0.0
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          port: 22
          SOURCE: ./docker-compose.yaml
          TARGET: ${{ secrets.DEPLOY_DIR }}/docker-compose.yaml
          # STRIP_SPOOFING: true # <-- Удалено

      # --- ШАГ 2: Копирование nginx.conf на сервер ---
      - name: Copy nginx.conf to server
        # Используйте ту же версию, что и выше
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          port: 22
          SOURCE: ./nginx.conf
          TARGET: ${{ secrets.DEPLOY_DIR }}/nginx.conf
          # STRIP_SPOOFING: true # <-- Удалено
      # --- ШАГ 3: Выполнение команд развертывания на сервере ---
      - name: Execute deploy script on Yandex Cloud VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          request_pty: true # Важно для корректной работы Docker команд
          port: 22
          script: |
            set -x # Включить вывод команд для отладки

            # Переходим в директорию проекта, где теперь лежат docker-compose.yaml и nginx.conf
            cd ${{ secrets.DEPLOY_DIR }}

            # Создаем .env файл с переменными окружения
            # Убедитесь, что все нужные переменные передаются как секреты GitHub!
            cat << 'EOF' > .env
            DB_NAME=${{ secrets.DB_NAME }}
            DB_USER=${{ secrets.DB_USER }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            DB_HOST=db # Имя сервиса в docker-compose.yml на сервере
            DB_PORT=5432
            STRIPE_SECRET_KEY=${{ secrets.STRIPE_SECRET_KEY }}
            STRIPE_PUBLISHABLE_KEY=${{ secrets.STRIPE_PUBLISHABLE_KEY }}
            CELERY_BROKER_URL=redis://redis:6379/0 # Имена сервисов для Docker Compose
            CELERY_RESULT_BACKEND=redis://redis:6379/0
            REDIS_HOST=redis
            REDIS_PORT=6379
            REDIS_DB=0
            DEBUG=False
            ALLOWED_HOSTS=${{ secrets.ALLOWED_HOSTS }}
            SECRET_KEY=${{ secrets.SECRET_KEY }}
            DOCKER_HUB_USERNAME=${{ secrets.DOCKER_HUB_USERNAME }}
            DOCKER_HUB_ACCESS_TOKEN=${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
            EOF

            # Login to Docker Hub
            echo "${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}" | docker login -u "${{ secrets.DOCKER_HUB_USERNAME }}" --password-stdin

            # Stop old containers (Nginx тоже будет остановлен)
            docker compose down || true

            # Pull latest images
            # Убедитесь, что IMAGE_NAME корректно передается и содержит полное имя образа
            docker pull "${{ env.IMAGE_NAME }}:latest" # Образ вашего приложения
            docker pull nginx:latest # Образ Nginx

            # Start containers (включая Nginx с монтированным конфигом)
            # Команды docker compose будут использовать docker-compose.yaml, который мы скопировали
            docker compose up -d

            # Run migrations (на сервисе web)
            # Убедитесь, что сервис, где запускаются миграции, называется 'web'
            docker compose exec web python manage.py migrate

            # Collect static files (на сервисе web)
            docker compose exec web python manage.py collectstatic --noinput

            # Restart application (web сервис, Nginx должен перезапуститься сам при изменении конфига,
            # но перезапуск web гарантирует, что он готов к работе)
            docker compose restart web